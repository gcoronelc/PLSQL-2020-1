SELECT * FROM EMPLEADO;
DECLARE
  P_EP_IDEMPLEADO NUMBER;
  P_AUMENTO NUMBER;
  P_ERROR VARCHAR2(200);
BEGIN
  P_EP_IDEMPLEADO := 110;
  P_AUMENTO := 100;

  SP_ACTUALIZARPAGO(
    P_EP_IDEMPLEADO => P_EP_IDEMPLEADO,
    P_AUMENTO => P_AUMENTO,
    P_ERROR => P_ERROR
  );
  /* Legacy output: 
DBMS_OUTPUT.PUT_LINE('P_ERROR = ' || P_ERROR);
*/ 
  :P_ERROR := P_ERROR;
--rollback; 
END;
/